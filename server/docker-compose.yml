services:
  tierlist-sync:
    build: .
    ports:
      - "${PORT:-3847}:3001"
    environment:
      - SYNC_TOKEN=${SYNC_TOKEN}
      - PORT=3001
    volumes:
      - tierlist-data:/data
    restart: unless-stopped

volumes:
  tierlist-data:
